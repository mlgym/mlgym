import { AnyKeyValuePairsInterface } from '../../experimentPage/ExperimentPage';
// import React, { useEffect, useState } from 'react';

export default function DatasetDetails({experiment_id, datasetDetails} : {experiment_id: string, datasetDetails: AnyKeyValuePairsInterface}) {

    return(
        <div>

        </div>
    );
}
